<h2>New Tweet!</h2>
<form action="/tweets" method="POST" class="tweet-compose" data-tweets-ul="#feed">
  Characters Left <strong class="chars-left">140</strong>
  <br>
  <input
      type="hidden"
      name="authenticity_token"
      value="<%= form_authenticity_token %>">

  <label>
    Content
    <textarea name="tweet[content]" id="tweet-input"></textarea>
  </label>
  <br>

  <label>
    Mention
    <script type="text/template">
      <select name="tweet[mentioned_user_ids][]" id="mention">
        <option></option>
        <% User.all.each do |user| %>
          <option value="<%= user.id %>"><%= user.username %></option>
        <% end %>
      </select>
    </script>
    <a href="javascript:void(0)" class="add-mentioned-user">Add Mention</a>
  </label>
  <br>

  <input type="Submit" value="Post Tweet!" id="tweet-submit">
</form>
